# Offene Aufgaben

- [ ] `react-chessboard` wirft manchmal beim Wechsel `setup`→`review` (Klick auf „Start Review") einen Fehler `Uncaught Error: Square width not found` aus der internen `<Piece>`-Komponente, der die ganze React-App zum Absturz bringt (leerer `#root`, nur ein Reload hilft). Bestätigt vorbestehend (2026-07-10, per `git stash`-Gegenprobe reproduziert auch auf unverändertem Code). Trat in der automatisierten Preview-Sandbox bei Standard-Viewport-Größe konsistent auf; mit größerem Viewport (1600×1000) nicht reproduzierbar — vermutlich eine `ResizeObserver`/Layout-Race in `react-chessboard` bei sehr kleinem/knappem Board-Container. Nicht untersucht, ob das auch in echten Nutzer-Browsern bei normaler Fenstergröße auftritt.
- [ ] Phase 8 manuell gegentesten: eine Practice-Partie (Start Review → „Practice from here") wirklich durchspielen — ein paar Züge ziehen, prüfen dass die Engine je Zug antwortet und die Eval-Bar mitläuft. (Konnte im automatisierten Preview nicht getestet werden, weil sich die @dnd-kit-Drag-Geste dort nicht auslösen ließ — Harness-Limit, kein Code-Bug. Puzzle-Flow + Engine-Antwortmechanismus sind bereits verifiziert.)
