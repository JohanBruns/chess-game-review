# Offene Aufgaben

- [x] Optikplan zu Ende machen
- [x] Bug fixen: Brett ist nicht immer richtig rotiert (2026-07-10). Root Cause: `handleLoadPgn`s `reviewingSide`-Mechanismus kam nur vom GamePicker-Recent-Games-Flow — `?pgn=`-Links und manuelles PGN-Paste hatten keinerlei Signal, wer der reviewte Spieler ist. Fix: neuer render-phase Block in `App.tsx` (nach dem `prevReviewAs`-Block), keyed auf `pgn`-Änderung — gleicht `localStorage['chess-username']`/URL-`username`-Param gegen die geparsten `whiteName`/`blackName`-PGN-Header ab und setzt `orientation` bei Treffer. Bekannte Restlücke (kein Bug, Daten-Limit): ein `?pgn=`-Link ganz ohne begleitenden Username-Kontext (weder URL-Param noch vorheriger GamePicker-Nutzung im selben Browser) hat kein Signal und bleibt beim `reviewAs`-Default — live verifiziert (positiv: mit `chess-username` in localStorage korrekt Schwarz-unten; negativ: ohne jedes Signal weiterhin Weiß-unten-Default, keine Regression).
- [x] Bug fixen: Explain-Modus Eval-Badge/Bar-Divergenz (2026-07-10). Root Cause: Coach-Bubble-Badge (`reviewEvalBadge`) und Eval-Bar (`viewEval`) wurden unabhängig voneinander berechnet — der Badge ignorierte `explainStep` komplett (kein `explainStepMate`-Countdown, kein `result`-Fallback), konnte daher von der Bar abweichen. Fix: `reviewEvalBadge` wird jetzt aus dem bereits fertig aufgelösten `viewEval` abgeleitet (`formatEvalBadge(viewEval)`, direkt nach der idle/best/explain-if/else-Kette in `App.tsx`) — eine einzige Quelle für beide UI-Elemente, kann nicht mehr auseinanderlaufen. Live verifiziert: Explain-Modus komplett durchgesteppt (Badge/Bar stimmen bei jedem Schritt überein, auch am Linienende), Best-Modus ebenfalls konsistent. 278 Tests grün, tsc/eslint sauber.
- [ ] **Neu gefunden beim Verifizieren (2026-07-10, nicht behoben — außerhalb des Scopes der beiden obigen Fixes):** `react-chessboard` wirft manchmal beim Wechsel `setup`→`review` (Klick auf „Start Review") einen Fehler `Uncaught Error: Square width not found` aus der internen `<Piece>`-Komponente, der die ganze React-App zum Absturz bringt (leerer `#root`, nur ein Reload hilft). **Bestätigt vorbestehend** — reproduziert identisch auch auf dem unveränderten Code vor den beiden Fixes oben (per `git stash`-Gegenprobe). Trat in der automatisierten Preview-Sandbox bei Standard-Viewport-Größe konsistent auf; mit größerem Viewport (1600×1000) nicht reproduzierbar — vermutlich eine `ResizeObserver`/Layout-Race in `react-chessboard` bei sehr kleinem/knappem Board-Container. Nicht untersucht, ob das auch in echten Nutzer-Browsern bei normaler Fenstergröße auftritt.
- [x] Mit Fable lösen: Warum dauert die Analyse so unglaublich lange, bis sie fertig gerechnet hat? → **Diagnostiziert + gefixt (2026-07-10, Commits `08c14d5` + `558b71e`):** Ursache war MultiPV=3 × feste Tiefe 15 ohne Zeitdeckel auf jeder der ~81 Stellungen (single-threaded WASM). Fix: Zeitdeckel pro Zug + Hash 128MB + Zwei-Pass-Analyse (Pass 1 alle Stellungen Single-PV ~300ms, Pass 2 nur Kandidaten MultiPV-3 ~1000ms). Plan mit Details: `C:\Users\Johan\.claude\plans\schau-dir-die-to-do-liste-valiant-hopcroft.md`
- [x] Analyse-Fix nachverifiziert (2026-07-10): Partie 171190174548 komplett neu analysiert und Paritäts-Regression bestätigt — weiterhin **0 Brilliant / genau 3 Great (Bxf8, Kg6, Rc4)**, exakt wie in der Referenzpartie. Absolute Timing-Zahlen aus diesem Lauf sind NICHT aussagekräftig (siehe Punkt unten) und wurden verworfen.
- [x] Schritt 3 aus dem Analyse-Plan (Multithreading) versucht + wieder verworfen (2026-07-10): Multi-Thread-Stockfish-Build (`stockfish-18-lite.js` aus dem npm-Paket) + COOP/COEP-Header (`vite.config.ts` dev/preview, geplanter `vercel.json`-Block für Prod) probeweise eingebaut. Live gemessen: Selbst mit `Threads=1` ist der Multi-Build pro `go`-Aufruf mehrere Sekunden langsamer als der Single-Thread-Build (vermutlich weil der pthread-fähige Build seinen Haupt-Thread selbst als proxied Worker mit Atomics.wait-Synchronisation laufen lässt — Fixkosten pro UCI-Turnaround, nicht nur bei echter Parallelisierung). Ein isolierter Rohtest (neuer Worker, nur `uci`/`isready` ohne App-Kontext) hat im automatisierten Preview-Browser über 10+ Sekunden gar nicht geantwortet. Batch-Analyse wurde dadurch 5-8× langsamer statt schneller. **Kompletter Revert** (useEngine.ts threading-Code entfernt, Multi-Build-Dateien aus `public/engine/` gelöscht, COOP/COEP aus `vite.config.ts` raus, kein `vercel.json` angelegt) — `git diff` zu vorher ist leer bis auf diesen TODO-Eintrag. Reine WASM-Turnaround-Zeiten in der automatisierten Preview-Sandbox waren generell auffällig langsam (auch beim Single-Build ~2s/Zug statt der erwarteten 300ms-Budgets) — vermutlich CPU-gedrosselte Sandbox, nicht repräsentativ für echte Nutzer-Browser. Reale Nutzer-seitige Performance des Zwei-Pass-Fixes (Schritte 1+2) bleibt daher ungeprüft, nur die Korrektheit (Paritäts-Regression) wurde bestätigt.
- [ ] Phase 8 manuell gegentesten: eine Practice-Partie (Start Review → „Practice from here") wirklich durchspielen — ein paar Züge ziehen, prüfen dass die Engine je Zug antwortet und die Eval-Bar mitläuft. (Konnte im automatisierten Preview nicht getestet werden, weil sich die @dnd-kit-Drag-Geste dort nicht auslösen ließ — Harness-Limit, kein Code-Bug. Puzzle-Flow + Engine-Antwortmechanismus sind bereits verifiziert.)
