# Offene Aufgaben

- [x] `react-chessboard` wirft manchmal beim Wechsel `setup`→`review` (Klick auf „Start Review") einen Fehler `Uncaught Error: Square width not found` aus der internen `<Piece>`-Komponente (`node_modules/react-chessboard/dist/index.esm.js`, `Piece`'s animation `useEffect`: liest `getBoundingClientRect().width` eines Square-Divs synchron und wirft, falls das `falsy` ist — kein Fallback in der Lib). Ohne Boundary bringt das die ganze React-App zum Absturz (leerer `#root`, nur Reload hilft).
  **Abgesichert (2026-07-10):** [BoardErrorBoundary.tsx](src/components/BoardErrorBoundary.tsx) um `BoardPanel` in `App.tsx` gelegt — fängt den Fehler ab und zeigt einen „Reload board"-Button statt die App abstürzen zu lassen.
  **Root Cause nicht abschließend gefunden:** ausführlich versucht zu reproduzieren — echter Chrome-Browser (via claude-in-chrome Extension) bei ~900×700, automatisierte Preview-Sandbox bei 1024×700 und 640×480, dazu ~15 schnelle Wiederholungszyklen (Start Review → Back → Start Review) sowie ein sofortiger Klick ohne jede Wartezeit direkt nach Analyse-Ende. Kein einziger Absturz reproduzierbar, keine Konsolenfehler. Vermutlich eine seltene, nicht zuverlässig auslösbare Timing-Race (React-Passive-Effect-Flush vs. Layout in der ursprünglichen Umgebung), keine deterministisch von der Viewport-Größe abhängige. Kein Upstream-GitHub-Issue dazu gefunden. Die Boundary greift unabhängig von der genauen Ursache.
- [ ] Phase 8 manuell gegentesten: eine Practice-Partie (Start Review → „Practice from here") wirklich durchspielen — ein paar Züge ziehen, prüfen dass die Engine je Zug antwortet und die Eval-Bar mitläuft. (Konnte im automatisierten Preview nicht getestet werden, weil sich die @dnd-kit-Drag-Geste dort nicht auslösen ließ — Harness-Limit, kein Code-Bug. Puzzle-Flow + Engine-Antwortmechanismus sind bereits verifiziert.)
